{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "managed-namespace.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "managed-namespace.resourceName" (dict "suffix" "managednamespaceconfiguration-editor-role" "context" $) }}
rules:
- apiGroups:
  - operator.medinvention.io
  resources:
  - managednamespaceconfigurations
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - operator.medinvention.io
  resources:
  - managednamespaceconfigurations/status
  verbs:
  - get
{{- end }}
